/**
 * App — Root component for the Chatbot Flow Builder.
 *
 * Renders the FlowBuilder which contains all the application logic.
 */

import FlowBuilder from './components/FlowBuilder';

function App() {
  return <FlowBuilder />;
}

export default App;
